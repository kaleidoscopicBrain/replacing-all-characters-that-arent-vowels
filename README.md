# replacing-all-characters-that-arent-vowels
Replacing all characters in a string(not case sensitive) that are not vowels and returns their number(amount)
#
return str.replaceAll("(?i)[^aeiou]", "").length();
